# Fitness & Bioimpedance Tracker — Handoff / Spec

Документ для переноса прототипа в полноценную серверную версию (Claude Code).
Текущий прототип — рабочий React-артефакт (`fitness-tracker.jsx`). Цель — сервер на Beget с БД, авторизацией и синхронизацией между устройствами.

-----

## 1. Что это и текущее состояние

Личный трекер силовых тренировок и состава тела (биоимпеданс). Один пользователь. Ввод весов/повторов, история, графики прогресса.

**Текущее состояние (продакшен):** React-приложение собирается через Vite (`src/`), бэкенд — PHP 8 + MySQL в `/api/`, деплой на Beget по push в ветку `claude/fitness-tracking-app-zMTWe` (`.github/workflows/deploy.yml`: build → rsync → миграции БД). Данные ходят через `src/api.js` (`fetch` + Bearer-токен), не через `window.storage`.

**Что уже сделано (бывшие ограничения прототипа закрыты):**

- ✅ **Синхронизация между устройствами** — данные в MySQL, общий бэкенд (`api/`).
- ✅ **Авторизация** — общий секретный токен, заголовок `Authorization: Bearer <TOKEN>` (`api/lib/Auth.php`), экран-гейт ввода токена в клиенте.
- ✅ **Бэкап** — ежедневные бэкапы Beget (30 дней) поверх MySQL.
- ✅ **Историческая выгрузка** убрана из клиента (`SEED_HISTORY` больше нет) — история сидится на стороне БД.
- ✅ **Нормализация названий на сервере** — канон применяется при сейве `POST /sessions` (`api/lib/Exercises.php`), а не только на клиенте.

**Что осталось / техдолг — см. §9 и §10.**

-----

## 2. Стек прототипа

- React (функциональные компоненты, hooks).
- `recharts` — графики.
- `lucide-react` — иконки.
- Шрифты: Bricolage Grotesque (заголовки), Hanken Grotesk (текст), JetBrains Mono (цифры).
- Тема: тёмная (charcoal `#0c0e0a`) + лаймовый акцент `#c8f23f`. Все CSS-переменные/классы в одном `<style>`-блоке внутри компонента + объект `C` для цветов в recharts.
- Хранилище: обёртка `store.get/set` над `window.storage` (JSON в строках).

-----

## 3. Функциональные требования (по вкладкам)

### Обзор (`Home`)

- Карточки-метрики: тренировок за 7 дней, всего тренировок, текущий вес (+дельта к прошлому замеру), жир % (+дельта).
- Дельты по весу/жиру: снижение = «хорошо» (зелёная стрелка вниз) — инвертированная логика.
- Блок «Последняя тренировка»: тип + дата + список упражнений (топ-вес и объём по каждому).
- Блок «Состав тела»: последний замер (6 метрик).
- Если данных нет — кнопки-заглушки, ведущие на нужную вкладку.

### Тренировки (`Log`)

- Выбор одного из 4 шаблонов (см. §6). Под каждым: дата последней такой тренировки («посл.: 17 мая» / «ещё не было») и бейдж **«следующая»** на той, что идёт по циклу.
- Логика «следующей»: цикл `h1t1 → h1t2 → h2t1 → h2t2 → (повтор)`. Берётся самая свежая сессия, следующая в массиве шаблонов. Нет сессий → первая.
- Поле даты (по умолчанию сегодня).
- Карточки упражнений с **преднастроенными целевыми весами/повторами** из программы (плейсхолдеры подсказывают диапазон, напр. «8–10»).
- Внутри упражнения: добавить/удалить подход; удалить упражнение целиком (если не делал — **важно**, т.к. цели преднастроены и иначе сохранятся плановые значения).
- Сохранение: тост-подтверждение «{тип} сохранена — {дата}». Пустые подходы отфильтровываются. Если всё пусто — подсказка.
- История (раскрывается): список сессий (тип, дата, суммарный объём, упражнения с подходами). Удаление через **инлайн-подтверждение** (двухшаговое), т.к. нативный `confirm()` заблокирован в песочнице — на сервере можно вернуть обычный confirm/модалку.
- Кнопка одноразового импорта вшитой истории (`SEED_HISTORY`) — **на сервере убрать**, заменить сидингом БД.

### Тело (`Body`) — биоимпеданс

- Форма замера: дата + 6 метрик (см. §4). Любое поле опционально.
- Замер на ту же дату **перезаписывается** (один замер в день).
- История замеров с инлайн-удалением.
- Тост-подтверждение сохранения.

### Прогресс (`Progress`)

- Выбор упражнения (dropdown, только встречавшиеся в истории) → график **объёма** или **макс. веса** по сессиям (bar).
- Состав тела: переключатель метрики → линейный график по датам.
- Упражнения матчатся **по точному названию** → отсюда требование единых названий (см. §5).

-----

## 4. Модель данных

```jsonc
// Сессия тренировки
{
  "id": "string",            // уникальный; в проде — UUID/autoincrement
  "date": "YYYY-MM-DD",
  "templateId": "h1t1|h1t2|h2t1|h2t2",
  "exercises": [
    {
      "n": "string",         // КАНОНИЧЕСКОЕ название упражнения
      "sets": [
        { "weight": 70, "reps": 8 }   // weight: число | "" (свой вес) | 0 (свой вес)
        // reps: число | "" | строка ("до отказа")
      ]
    }
  ]
}

// Замер биоимпеданса
{
  "id": "string",
  "date": "YYYY-MM-DD",      // уникальна (upsert по дате)
  "weight": 0,  "fat": 0,  "muscle": 0,
  "water": 0,   "visceral": 0, "bone": 0,
  "note": ""                 // все метрики: число | null
}
```

Биометрики (ключ/подпись/ед.изм.):
`weight`/Вес/кг · `fat`/Жир/% · `muscle`/Мышцы/кг · `water`/Вода/% · `visceral`/Висц. жир/— · `bone`/Кости/кг.

### Бизнес-правила

- **Объём упражнения** = Σ(weight × reps) по подходам (свой вес → 0).
- **Топ-вес** = max(weight) по подходам.
- **Дельта** на дашборде = последний замер − предыдущий.
- Невалидные числа → трактуются как 0/null (через хелпер `num`).

-----

## 5. Канонические названия упражнений (важно!)

Из-за разных формулировок в Excel и в программе возникали дубли. Введён словарь синонимов → канон.

**Реализовано на двух уровнях:**
- Клиент — `CANON` / `canon()` / `normSession()` в `src/data.js`, применяется при сейве.
- Сервер — `Exercises::CANON` / `canon()` / `normExercises()` в `api/lib/Exercises.php`, применяется в `POST /sessions` независимо от клиента (UI, прямой API, сид). Склейка подходов с одинаковым каноном внутри сессии — тоже на сервере.

⚠️ **Словарь дублируется в JS и PHP — держать синхронными.** При изменении синонимов править оба места. Возможный шаг на будущее: вынести справочник в таблицу `exercise_aliases` в БД (схема в §8) и читать оттуда на обоих уровнях.

Единый формат: **Действие → Чего → Куда/как** (жим штанги лёжа, тяга вертикального блока к груди). Исключения — устоявшиеся названия (подтягивания, брусья, гиперэкстензия).

|Синонимы (входящие)                                                       |Канон                                       |
|--------------------------------------------------------------------------|--------------------------------------------|
|Жим лёжа                                                                  |**Жим штанги лёжа**                         |
|Жим гантелей 15°; Жим гантелей лёжа; Жим гантелей под углом 30°            |**Жим гантелей на скамье 15°**              |
|Жим в тренажёре на грудь (1 рука)                                         |**Жим в тренажёре сидя на грудь**           |
|Тяга верт. блока к груди                                                  |**Тяга вертикального блока к груди**        |
|Тяга гориз. блока к поясу                                                 |**Тяга горизонтального блока к поясу**      |
|Протяжка к подбородку; Протяжка штанги к подбородку                       |**Тяга штанги к подбородку**                |
|Бицепс штанга                                                             |**Подъём штанги на бицепс**                 |
|Молотковые; Молотковые подъёмы гантелей                                   |**Подъём гантелей хватом «молот»**          |
|Подъёмы на носки сидя; Подъёмы на носки в тренажёре сидя                  |**Подъём на носки в тренажёре сидя**        |
|Подъём ног в висе / dead bug; …или dead bug                              |**Подъём ног в висе**                       |
|Разведения лёжа; Разведения гантелей лёжа                                 |**Разведение гантелей лёжа**                |
|Махи в стороны                                                            |**Махи гантелей в стороны**                 |
|Бабочка / сведения; Сведения в тренажёре (бабочка); Сведения в кроссовере |**Сведение рук в тренажёре**                |
|Трицепс канат; Трицепс в канате; Трицепс в блоке / канат                  |**Разгибание на трицепс на блоке (канат)**  |
|Выпрыгивания с гантелью                                                   |**Выпрыгивания с гантелей**                 |
|Гоблет-присед                                                             |**Приседания с гантелью (гоблет)**          |
|Пресс в тренажёре                                                         |**Скручивания в тренажёре**                 |
|Отжимания на брусьях                                                      |**Брусья**                                  |
|Подтягивания в гравитроне                                                 |**Подтягивания**                            |
|Гиперэкстензия классическая                                               |**Гиперэкстензия**                          |

`normSession()` дополнительно объединяет упражнения с одинаковым каноном **внутри одной сессии** (склеивает подходы).

**Слияния (потеря нюанса), подтверждены на ревью продукта:**

1. «Жим гантелей на скамье 15°» включает плоский (лёжа) и 30° наклон.
1. «Сведение рук в тренажёре» = бабочка + кроссовер (разные тренажёры).
1. «Подтягивания» = свободные + гравитрон (в гравитроне число = вес помощи, смысл нагрузки обратный).
1. «Разгибание на трицепс на блоке (канат)» объединяет «Трицепс канат» и «Трицепс в канате».

«Жим гантелей сидя» оставлен **отдельно** (это плечи, не грудь). «Мёртвый жук» отвязан от «Подъём ног в висе» и **убран из шаблона** — добавляется вручную при желании.

-----

## 6. Программа тренировок (4 шаблона)

Преднастроенные цели для логирования. Формат подхода: `вес×повторы` (диапазон → в подсказке). Названия — **канонические** (§5).

**Н1·Т1 (грудь/спина/руки):** Выпрыгивания с гантелей 12×5 ×4 · Жим штанги лёжа 60×8/70×5/70×5(4–5)/70×4 · Разведение гантелей лёжа 14×10 ×3 · Подтягивания 6 б/п / 5–6 б/п / помощь 14×8–10 · Махи гантелей в стороны 9×12 ×3 · Подъём штанги на бицепс 20×10/20×10/17.5×10 · Подъём ног в висе 12×3

**Н1·Т2 (ноги/плечи/трицепс):** Подъём на носки в тренажёре сидя 20×15/20×15/25×12 · Жим гантелей на скамье 15° 22×10 ×3 · Тяга вертикального блока к груди 45×12/45×12/45×10 · Брусья 12 б/п / 8–10 б/п / помощь 9×9–10 · Жим гантелей сидя 16×10 ×3 · Разгибание на трицепс на блоке (канат) 11.3×12/11.3×12/13.5×8–10 · Скручивания в тренажёре 12×3

**Н2·Т1 (грудь/спина/руки):** Приседания с гантелью (гоблет) 26×8 ×4 · Жим гантелей на скамье 15° 22×10/24×8/24×8 · Сведение рук в тренажёре 15×12/15×10–12 ×2 · Подтягивания 8 б/п / 5 б/п / помощь 14×8–10 · Тяга штанги к подбородку 25×10 ×3 · Подъём гантелей хватом «молот» 10×10/10×10/7.5×10 · Подъём ног в висе ×3

**Н2·Т2 (ноги/плечи/руки):** Подъём на носки в тренажёре сидя 20×15/25×12/25×12 · Жим в тренажёре сидя на грудь 30×10/30×10/32.5×8–10 · Тяга горизонтального блока к поясу 45×10/45×10/50×8–10 · Брусья 10/10/9–10 б/п · Жим гантелей сидя 16×10/18×8/18×8 · Подъём штанги на бицепс 25×10 ×2/25×8–9 · Скручивания в тренажёре 12×3

Точные массивы целей — в константе `TEMPLATES` в `fitness-tracker.jsx`.

-----

## 7. Импортированная история (контекст)

10 тренировок (21.03.2026 – 17.05.2026) загружены из `fitness-trains.xlsx`. Нюансы парсинга — учесть при будущих импортах:

- Дата в Excel — serial number (конвертация: `1899-12-30 + N дней`).
- **Excel мангленил десятичные веса в даты** (`11.3` → «11 мар»). Восстановлено правилом `день.месяц`, сверено с программой (Трицепс 11.3/13.5/12.5; жим тренажёр 27.5; брусья 8.5 повт). При новых импортах: колонки веса/повторов в Excel держать в формате **Текст/Число**, иначе повторится.
- `session_id` одной записи содержал «2025-03-27», но колонка даты = 2026-03-27 → доверяем колонке даты.
- Колонка `exercise_order` содержала мусор — игнорируется.
- Колонки исходника: `session_id, date, week, training, exercise_order, exercise, set_no, weight_kg, reps`.
- `week`(1/2) + `training`(1/2) → `templateId` (`h{week}t{training}`).

На сервере историю **засидить в БД миграцией** (из `SEED_HISTORY` в коде или заново распарсив xlsx), а не вшивать в клиент.

-----

## 8. Целевая архитектура (сервер, Beget)

Beget поддерживает: PHP 8.x, MySQL, PostgreSQL, Node.js, phpMyAdmin, бесплатный SSL, ежедневные бэкапы 30 дней.

### Рекомендация

- **Фронтенд:** тот же React, собрать через **Vite** в статику, выложить в корень домена. Логику (шаблоны, канон, цикл, расчёты) перенести как есть.
- **Бэкенд (основной вариант — наименьшее трение на shared-хостинге):** **PHP 8 + MySQL**, API в `/api/` того же домена (без CORS). PDO + prepared statements.
- **Альтернатива:** Node.js + SQLite/MySQL (если возьмёшь VPS и хочешь единый JS-стек).
- **Хранилище данных в JSON-колонке хранить как `TEXT`** (Beget MySQL может быть 5.x без native JSON).

### Замена хранилища

✅ Сделано: доступ к данным идёт через модуль `src/api.js` (`fetch` + Bearer-токен) вместо прежней обёртки `store`/`window.storage`. Структура состояния и компоненты те же.

### API-контракт (REST, бэкенд-агностичный)

```
Авторизация: заголовок  Authorization: Bearer <TOKEN>   (общий секрет в env)

GET    /api/sessions            -> [Session]
POST   /api/sessions            <- Session   (upsert по id)
DELETE /api/sessions/{id}

GET    /api/bio                 -> [BioEntry]
POST   /api/bio                 <- BioEntry   (upsert по date)
DELETE /api/bio/{id}

GET    /api/export              -> { sessions:[...], bio:[...] }   (бэкап)
POST   /api/import              <- { sessions:[...], bio:[...] }   (восстановление/миграция)
```

### Схема БД (MySQL)

```sql
CREATE TABLE sessions (
  id          VARCHAR(40) PRIMARY KEY,
  date        DATE NOT NULL,
  template_id VARCHAR(8) NOT NULL,
  data        TEXT NOT NULL,          -- JSON: exercises[]
  created_at  TIMESTAMP DEFAULT CURRENT_TIMESTAMP,
  INDEX (date), INDEX (template_id)
);

CREATE TABLE bio_entries (
  id         VARCHAR(40) PRIMARY KEY,
  date       DATE NOT NULL UNIQUE,    -- один замер в день
  weight     DECIMAL(5,1), fat DECIMAL(4,1), muscle DECIMAL(5,1),
  water      DECIMAL(4,1), visceral DECIMAL(4,1), bone DECIMAL(4,1),
  note       TEXT,
  created_at TIMESTAMP DEFAULT CURRENT_TIMESTAMP
);

-- опционально: справочник единых названий
CREATE TABLE exercise_aliases (
  alias  VARCHAR(120) PRIMARY KEY,
  canon  VARCHAR(120) NOT NULL
);
```

### Безопасность (обязательно)

- Без авторизации API на публичном домене = любой читает/пишет твои данные.
- Для одного пользователя достаточно **общего секретного токена** (в env, проверка на сервере) или **Basic Auth через `.htaccess`**.
- HTTPS включить (бесплатный SSL Beget).
- Не коммитить токен/доступы к БД в репозиторий.

### Надёжность / бэкап

- Данные в БД + ежедневные бэкапы Beget (30 дней).
- Эндпоинты `/export` + `/import` как ручной бэкап и инструмент миграции с прототипа.

-----

## 9. План (статус)

**Сделано:**

- ✅ Vite-проект (`src/App.jsx`, `src/data.js`), `recharts`, `lucide-react`, шрифты.
- ✅ Модуль `src/api.js` (fetch + токен) вместо `store.get/set`.
- ✅ Экран ввода токена (gate) + локальное хранение токена.
- ✅ Бэкенд PHP `/api/*`, PDO + MySQL, prepared statements, проверка токена (`api/lib/Auth.php`).
- ✅ Миграции БД (`db/`), применяются на деплое (`api/migrate.php`).
- ✅ Нормализация названий на сервере при сейве (`api/lib/Exercises.php`, §5).
- ✅ Сборка Vite → статика, API в `/api/`, SSL, деплой на Beget (`.github/workflows/deploy.yml`).
- ✅ `SEED_HISTORY` убран из клиента.
- ✅ Подтверждение удаления — **инлайн** (осознанное UX-решение, нативный `confirm` не возвращали).

**Остаётся (техдолг / по аппетиту):**

- ⬜ `/export` и `/import` как ручной бэкап/миграция (сейчас не реализованы; бэкап держится на ежедневных снапшотах Beget).
- ⬜ Push «фаза B» — серверные напоминания + сигнал таймера при заблокированном экране (Service Worker + Web Push). В настройках уже зарезервированы `pushOptIn` / `pushHour` / `pushThresholdDays`.
- ⬜ Апгрейд `recharts` v2 → v3 (v2 помечена deprecated; в v3 breaking changes).
- ⬜ Возможно — вынести словарь синонимов в таблицу БД, чтобы не дублировать JS/PHP (§5).

-----

## 10. Открытые продуктовые решения

- Слияния названий (§5, п.1–4) — **подтверждены на ревью продукта.**
- Импорт xlsx — **не нужен как постоянная фича** (договорились: ввод только в приложении).
- ✅ Оценка 1ПМ (формула Эпли) — **реализована** (e1RM в личных рекордах и графиках).
- Фичи на будущее: цели по весу/жиру с прогресс-баром, экспорт в CSV, авто-подсказка следующей тренировки по плану-циклу.

-----

## 11. Файлы

**Фронтенд (`src/`):**
- `App.jsx` — UI: вкладки, компоненты, экран-гейт токена, таймер отдыха, стрик.
- `data.js` — источник истины по логике фронта: `BUILTIN_TEMPLATES`, `CANON`/`canon`/`normSession`, расчёты (e1RM, объём, рекорды, стрик), `SETTINGS_DEFAULTS`, стили (`CSS`).
- `api.js` — клиент REST API (fetch + Bearer-токен).

**Бэкенд (`api/`):**
- `index.php` — фронт-контроллер, маршруты `/sessions` `/bio` `/templates` `/settings`.
- `lib/Auth.php`, `lib/Db.php`, `lib/Response.php` — авторизация, PDO, JSON-ответы.
- `lib/Exercises.php` — серверная канонизация названий (зеркало `CANON` из `data.js`).
- `migrate.php`, `config.php` — миграции и конфиг (секреты в `config.local.php`, не в репо).

**БД (`db/`):** `schema.sql` + `migration_00X_*.sql`.

**Прочее:** `.github/workflows/deploy.yml` — деплой на Beget; `HANDOFF.md` — этот документ.
